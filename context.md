# Food Cart App - Context & Style Guide

## 📌 ข้อมูลโปรเจกต์เบื้องต้น (Project Overview)
- **ประเภทแอป:** แอปพลิเคชันค้นหารถขายของเคลื่อนที่ (Food Cart App)
- **แพลตฟอร์ม:** Mobile App (พัฒนาด้วย Flutter)
- **ระบบหลังบ้าน (Backend):** Node.js (Express) + MySQL
- **IP Address ปัจจุบันของ Backend:** `http://192.168.1.3:3000`

## 🎨 สไตล์และแนวทางการออกแบบ (Design System & UI Guidelines)
เพื่อให้แอปมีหน้าตา (UI) และประสบการณ์ใช้งาน (UX) ที่ไปในทิศทางเดียวกัน โปรดใช้แนวทางการออกแบบดังนี้:

### 1. โทนสีหลัก (Color Palette)
แอปจะเน้นใช้โทนสี **ฟ้า-น้ำเงิน (Blue/Teal)** เป็นหลัก เพื่อให้ความรู้สึกทันสมัย สดใส และน่าเชื่อถือ
- **Primary Color (สีปุ่มกดหลัก):** `#00C7E6` (สีฟ้าสว่าง)
- **Secondary / Accent (สีปุ่มรอง/แถบสี):** `#32C6E8` (สีฟ้าอ่อน)
- **Dark Text / Heading (สีหัวข้อ/ตัวหนังสือหลัก):** `#0B1A30` (สีกรมท่าเข้ม/ดำอมน้ำเงิน)
- **Sub-text (ข้อความรอง/คำอธิบาย):** `#64748B` หรือ `Colors.black45` (สีเทา)
- **Background (พื้นหลังแอป):** `#F4F7FA` หรือ `#F9F9F9` (สีขาวอมเทาสว่าง สบายตา)
- **Input Fill Color (สีพื้นหลังช่องกรอกข้อมูล):** `#F4F5F7` หรือ `#F8FAFC`

### 2. รูปแบบปุ่มกด (Buttons)
- ใช้ปุ่มแบบขอบมน (Rounded Corners) เสมอ โดยตั้งค่า `borderRadius: BorderRadius.circular(25)` หรือ `30`
- ปุ่มหลัก (ElevatedButton) จะใช้สีฟ้า `#00C7E6` ข้อความสีขาว หนา (Bold) และมีเงา (Elevation) อ่อนๆ
- ปุ่มตัวหนังสือ (TextButton) สำหรับข้อความที่กดได้ (เช่น ลืมรหัสผ่าน, สมัครสมาชิก) ให้ใช้ข้อความสี `#0B1A30` เพื่อความเรียบหรู

### 3. รูปแบบช่องกรอกข้อมูล (TextFields)
- ใช้ดีไซน์แบบไม่มีเส้นขอบ (No Border) โดยตั้ง `borderSide: BorderSide.none`
- ขอบมุมโค้งมน `BorderRadius.circular(16)` ถึง `25`
- ใช้สีพื้นหลังของช่อง (Filled Color) เป็นสีเทาอ่อน `#F4F5F7` เพื่อความ Minimal
- ไอคอน (Prefix/Suffix Icon) ให้ใช้เป็นลักษณะลายเส้นโปร่ง (Outlined Icons) สีเทา

### 4. โครงสร้างและเงา (Layout & Shadow)
- กล่องข้อความหรือฟอร์ม มักจะใส่พื้นหลังสีขาว ตัดกับพื้นหลังแอปสีเทาอ่อน
- **เงา (Shadow):** ใช้เงานุ่มๆ เบาๆ มากๆ (เช่น `blurRadius: 24`, `color: Colors.black.withOpacity(0.03)`) เพื่อให้กล่องดูลอยขึ้นมาแบบพรีเมียม ไม่ใช้เงาแข็งๆ

## 🚀 ข้อกำหนดสำหรับ AI (AI Instructions)
หากให้ AI ช่วยเขียนโค้ดหน้าจอ UI ใหม่ **ต้องยึดสไตล์, โทนสี, และความโค้งมนด้านบนนี้เป็นหลักเสมอ** ห้ามใช้ดีไซน์พื้นฐาน (Default Material) เด็ดขาด เพื่อรักษาความสม่ำเสมอ (Consistency) ของแอปไว้.

---

# สถานะการเชื่อมฐานข้อมูล — ฝั่งลูกค้า

ตรวจสอบจาก Flutter ใน `lib` และ Backend ใน `/Users/natchanan/Desktop/Backend` เมื่อวันที่ 3 กันยายน 2026

คำอธิบายสถานะ:

- **เชื่อมแล้ว** — Flutter เรียก API และ Backend อ่านหรือเขียน MySQL แล้ว
- **เชื่อมบางส่วน** — มี API หรือเชื่อมบางข้อมูลแล้ว แต่ยังมีค่า mock/hardcode หรือข้อมูลผู้ใช้ส่งต่อไม่ครบ
- **ยังไม่เชื่อม** — UI เปลี่ยนเฉพาะ State ภายในแอป หรือแสดงข้อมูลจำลองโดยไม่เรียก API

## เชื่อมฐานข้อมูลแล้ว

| ระบบ | สถานะ | รายละเอียด |
| --- | --- | --- |
| สมัครสมาชิกลูกค้า | เชื่อมแล้ว | `register_buyer_screen.dart` เรียก `POST /api/customers/register` และ Backend บันทึกลงตาราง `customer` |
| Login ลูกค้า | เชื่อมแล้วเฉพาะการตรวจสอบบัญชี | `login_screen.dart` เรียก `POST /api/customers/login` และ Backend ตรวจข้อมูลในตาราง `customer` |
| ส่ง OTP | เชื่อม API แล้ว | `ForgotPassword_screen.dart` เรียก `POST /api/otp/send` แต่ OTP เก็บชั่วคราวในหน่วยความจำของ Backend ไม่ได้เก็บใน MySQL |
| ตรวจสอบ OTP | เชื่อม API แล้ว | `otp_verification_screen.dart` เรียก `POST /api/otp/verify` |
| ตั้งรหัสผ่านใหม่จากระบบลืมรหัสผ่าน | เชื่อมแล้ว | `create_new_password_screen.dart` เรียก `POST /api/customers/reset-password` และ Backend อัปเดตตาราง `customer` |
| ดึงร้านค้าและเมนูพื้นฐาน | เชื่อมแล้วบางข้อมูล | `buyer_home_screen.dart` เรียก `GET /api/merchants/trucks` และ Backend อ่านตาราง `merchant` กับ `menus` |
| ดึงประวัติคำสั่งซื้อ | เชื่อม API แล้ว | `buyer_home_screen.dart` เรียก `GET /api/orders/customer/:customerId` และ Backend อ่าน `orders`, `order_items`, `merchant` และ `reviews` |
| ส่งรีวิวจากประวัติคำสั่งซื้อ | เชื่อม API แล้ว | เรียก `POST /api/orders/review` เพื่อเพิ่มข้อมูลใน `reviews` และอัปเดต `orders.is_rated` |

## เชื่อมฐานข้อมูลบางส่วนหรือยังทำงานไม่ครบ

### 1. การส่งข้อมูลลูกค้าหลัง Login

- Backend ส่งข้อมูลลูกค้ากลับมาใน `data['customer']`
- แต่ `login_screen.dart` เปิด `BuyerHomeScreen()` โดยไม่ได้ส่งข้อมูลดังกล่าว
- ผลกระทบคือหน้าหลักไม่ทราบ `customer_id` ของบัญชีที่ Login จริง
- ต้องแก้ส่วนนี้ก่อนระบบออเดอร์ รีวิว โปรไฟล์ ร้านที่ติดตาม การตั้งค่า และการลบบัญชี

### 2. ประวัติคำสั่งซื้อ

- เชื่อม API แล้ว
- หากไม่มีข้อมูลผู้ใช้ ระบบใช้ `customer_id = 1` เป็นค่าเริ่มต้น
- อาจทำให้บัญชีอื่นดึงประวัติของลูกค้าหมายเลข 1
- จุดตรวจสอบ: `buyer_home_screen.dart` ฟังก์ชัน `_fetchMyOrdersFromDB()`

### 3. โปรไฟล์ลูกค้า

- มี `GET /api/customers/:id` และ `PUT /api/customers/:id`
- Flutter อ่าน ID จากคีย์ `id` แต่ Backend ส่งคีย์ `customer_id`
- การดึงและแก้ไขโปรไฟล์จึงอาจหยุดเพราะหา ID ไม่พบ
- Backend อัปเดตเฉพาะชื่อ อีเมล และเบอร์โทร
- วันเกิด รูปโปรไฟล์ และที่อยู่ยังไม่ได้บันทึกจากหน้าแก้ไขโปรไฟล์
- จุดตรวจสอบ: `buyer_profile_screen.dart` และ `edit_buyer_profile_screen.dart`

### 4. ข้อมูลร้านค้าและเมนู

ข้อมูลที่มาจากฐานข้อมูล:

- ID ร้าน
- ชื่อร้าน
- ประเภทร้าน
- ชื่อเมนู
- ราคาเมนู
- URL รูปเมนู

ข้อมูลที่ยัง hardcode หรือจำลอง:

- คะแนนร้าน
- จำนวนรีวิว
- รูปร้าน
- สถานะเปิดหรือปิด
- ระยะทาง
- รายละเอียดร้าน
- เวลาเปิดร้าน
- หมวดหมู่เมนู
- คำอธิบายเมนู
- ตัวเลือกเมนูและราคาเพิ่มเติม

### 5. รีวิวร้านค้า

- การส่งรีวิวจากประวัติออเดอร์เรียก API แล้ว
- แต่ยังส่ง `customer_id: 1` และใช้ `merchant_id: 1` เป็นค่าทดแทนเมื่อไม่มีข้อมูล
- หน้า `ReviewsPage` เพิ่มรีวิวลง List ภายในเครื่อง ไม่ได้บันทึกฐานข้อมูล
- Backend ยังไม่มี endpoint สำหรับดึงรีวิวทั้งหมดตามร้าน
- รูปรีวิวที่เลือกยังไม่ได้อัปโหลดผ่าน `/api/orders/upload-image` ก่อนบันทึกรีวิว

## ยังไม่เชื่อมฐานข้อมูล

| ระบบ | สภาพปัจจุบัน | Backend ปัจจุบัน |
| --- | --- | --- |
| สร้างคำสั่งซื้อ | หน่วงเวลา 2 วินาทีและสร้างเลข `ORD-MOCKUP` โดยไม่เรียก API | มี `POST /api/orders/create` แล้ว แต่ Flutter ยังไม่ได้เรียก |
| ตะกร้าสินค้า | เก็บใน Map ภายใน `buyer_home_screen.dart`; ปิดแอปแล้วข้อมูลหาย | ยังไม่มี API สำหรับบันทึกตะกร้า |
| ติดตาม/ยกเลิกติดตามร้าน | เปลี่ยน `_isFollowing` เฉพาะใน State | มี `POST /api/customers/follow` แล้ว แต่ Flutter ยังไม่ได้เรียก |
| รายชื่อร้านที่ติดตาม | นำร้าน 3 ร้านแรกจากร้านทั้งหมดมาแสดง | มี `GET /api/customers/:customerId/followed` แล้ว แต่ Flutterยังไม่ได้เรียก |
| จำนวนผู้ติดตามร้าน | เริ่มต้นด้วยเลข `154` และบวกหรือลบใน State | มี API นับผู้ติดตามร้าน แต่ Flutter ยังไม่ได้เรียก |
| การแจ้งเตือน | รายการแจ้งเตือนเป็นข้อมูล hardcode | ยังไม่มีตารางหรือ API แจ้งเตือน |
| ตั้งค่าการแจ้งเตือน | Switch เก็บค่าใน `_storeNotifications` เท่านั้น | ยังไม่มีตารางหรือ API ตั้งค่าการแจ้งเตือน |
| เปลี่ยนรหัสผ่านจากหน้าตั้งค่า | ตรวจช่องกรอกแล้วแสดง Dialog สำเร็จโดยไม่เรียก API | ยังไม่มี endpoint สำหรับตรวจรหัสเดิมแล้วเปลี่ยนรหัสผ่าน |
| ลบบัญชี | ปุ่มยืนยันเพียงปิด Dialog | มี `DELETE /api/customers/:id` แล้ว แต่ Flutter ยังไม่ได้เรียก |
| ที่อยู่ลูกค้า | ข้อความตำแหน่งเก็บใน State | ตารางมี `current_address` แต่ Flutter ยังไม่อ่านหรือบันทึก |
| แผนที่และพิกัด | ใช้แผนที่จำลองและตำแหน่งชั่วคราว | ยังไม่มีโครงสร้างจัดเก็บพิกัดลูกค้า/ร้านที่ครบถ้วน |
| ระบบชำระเงิน | QR ใช้ข้อมูล `ThaiQRPayment_Mock` | ยังไม่มี API หรือตารางธุรกรรมการชำระเงิน |
| ระบบคืนเงิน | มีหน้าจอแสดงผล แต่ไม่พบการเรียก API | ยังไม่มี API หรือตารางคำขอคืนเงิน |
| ร้านโปรดใน `buyer_profile_screen.dart` | เป็นรายการ hardcode แยกจากระบบร้านที่ติดตาม | ยังไม่ได้เชื่อม API |
| การแจ้งเตือนใน `buyer_profile_screen.dart` | เป็นรายการ hardcodeอีกชุดหนึ่ง | ยังไม่ได้เชื่อม API |

## Endpoint ที่ Backend มีแล้ว แต่ Flutter ยังไม่ได้ใช้ครบ

| Endpoint | งานที่รองรับ | สถานะการใช้จาก Flutter |
| --- | --- | --- |
| `POST /api/orders/create` | สร้างออเดอร์และรายการสินค้า | ยังไม่เรียก |
| `PUT /api/orders/:id/complete` | เปลี่ยนสถานะเป็นรับอาหารสำเร็จ | ยังไม่พบการเรียก |
| `POST /api/orders/upload-image` | อัปโหลดรูปรีวิว | ยังไม่พบการเรียก |
| `POST /api/customers/follow` | ติดตามหรือยกเลิกติดตามร้าน | ยังไม่เรียก |
| `GET /api/customers/:customerId/followed` | ดึง ID ร้านที่ลูกค้าติดตาม | ยังไม่เรียก |
| `DELETE /api/customers/:id` | ลบบัญชีลูกค้า | ยังไม่เรียก |
| `GET /api/merchants/:merchantId/followers` | ดึงจำนวนผู้ติดตามร้าน | ยังไม่เรียก |

## ลำดับการเชื่อมที่แนะนำ

1. ส่ง `customer` จากหน้า Login เข้า `BuyerHomeScreen`
2. ทำให้ทุกหน้ารองรับคีย์ `customer_id` ให้ตรงกับ Backend
3. เชื่อมปุ่มสร้างคำสั่งซื้อกับ `POST /api/orders/create`
4. ยกเลิกค่าเริ่มต้น `customer_id = 1` และ `merchant_id = 1`
5. เชื่อมติดตามร้าน รายชื่อร้านที่ติดตาม และจำนวนผู้ติดตาม
6. แก้การดึงและอัปเดตโปรไฟล์ รวมวันเกิด รูป และที่อยู่
7. เพิ่ม API ดึงรีวิวร้านและเชื่อมอัปโหลดรูปรีวิว
8. เชื่อมเปลี่ยนรหัสผ่านและลบบัญชี
9. ออกแบบตาราง/API สำหรับการแจ้งเตือน การตั้งค่า การชำระเงิน และการคืนเงิน

## หมายเหตุ

- รายงานนี้ครอบคลุมเฉพาะฝั่งลูกค้าเป็นหลัก
- ยังไม่มีการแก้ Logic หรือเชื่อม API เพิ่มเติมจากการจัดทำรายงานนี้
- ก่อนแก้แต่ละระบบควรตรวจ schema ตาราง MySQL จริง เพื่อไม่สมมติชื่อ Table หรือ Column
